# Neural Style Transfer (TensorFlow + TF-Hub)

This project performs **Arbitrary Neural Style Transfer** using TensorFlow Hub’s pretrained model  
`magenta/arbitrary-image-stylization-v1-256`.

The script works on **any local machine**, **VS Code**, **PyCharm**, or any normal Python environment.

---

## 📦 Requirements

Install dependencies:

```bash
pip install tensorflow tensorflow-hub pillow matplotlib requests
r
